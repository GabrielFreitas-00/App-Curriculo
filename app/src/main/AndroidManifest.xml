<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.curriculo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cv"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Contato"
            android:parentActivityName=".MainActivity"
            android:label="@string/app_contato"></activity>
        <activity android:name=".Diferenciais"
            android:parentActivityName=".MainActivity"
            android:label="@string/app_nameDiferenciais"/>
        <activity android:name=".Educacao"
            android:parentActivityName=".MainActivity"
            android:label="@string/app_nameEducacao"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>